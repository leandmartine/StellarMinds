namespace StellarMindsMVC.Models;

// Espejo del enum IndicadorAdecuacion del backend (StellarMinds.Entidades.Enums).
// La API lo serializa como número, por lo que el orden de los valores debe coincidir.
public enum IndicadorAdecuacion
{
    IDEAL,
    ADECUADO,
    NO_RECOMENDABLE
}
